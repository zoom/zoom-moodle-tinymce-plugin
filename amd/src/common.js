/**
 * Common values for tiny_zoomclassroom.
 *
 * @module     tiny_zoomclassroom/common
 */

const component = 'tiny_zoomclassroom';

export {
    component,
};

export const pluginName = `${component}/plugin`;
export const icon = component;
export const buttonName = `${component}_button`;
